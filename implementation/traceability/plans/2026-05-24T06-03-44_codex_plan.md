## Goal

Create an inspect-only workflow smoke-test plan for `REVIEW_ENABLED=0`, confirming from workflow logic that the runner can skip Claude review, create/use a skipped-review artifact, and pass that artifact into the Codex decision step.

## Repo Context Checked

Cheap workflow inventory only: `tools/agent-review`, `.claude/settings.json`, and workflow management docs surfaced by targeted `find`.

## Planning Evidence Read

- Files actually read:
  - `tools/agent-review/runner/run_plan_review.sh`
  - `.claude/settings.json`
  - `docs/project-management/RUNIAC_AGENT_REVIEW_TODO.md`
  - `tools/agent-review/profiles/runiac/prompts/03_codex_final_review_decision.md`

- Files intentionally skipped:
  - Product requirements, PDD content, diagrams, PDFs, images, Flutter/Firebase source, tests, generated assets, and existing review artifacts.

- Reason for skipping large/reference/irrelevant files:
  - This is a workflow smoke test only. The runner and decision prompt are enough to assess skipped-review behavior without broad repository context.

## Context Class Decision

- Selected context class: `workflow`
- Reason: The task concerns agent-review runner behavior, review skipping, and Codex decision flow.
- Excluded classes and why:
  - `docs`: not a documentation content task.
  - `implementation_prep`: no implementation planning requested.
  - `feature`: no app feature behavior requested.
  - `security`: no rules/secrets/auth changes requested.
  - `architecture`: no system design changes requested.
  - `unknown`: task scope is clear enough.
- Whether user-declared or Codex-inferred: Codex-inferred.

## Plan Scope

- Allowed planning files/paths:
  - `tools/agent-review/runner/run_plan_review.sh`
  - `tools/agent-review/profiles/runiac/prompts/03_codex_final_review_decision.md`
  - `.claude/settings.json`
  - `docs/project-management/RUNIAC_AGENT_REVIEW_TODO.md` only as workflow context

- Excluded planning files/paths:
  - Flutter, Firebase, backend, tests, production source, PDD deliverables, submissions, diagrams, generated assets, binaries, secrets, `.env*`.

- Large or expensive assets to avoid:
  - PDFs, images, generated assets, build outputs, `.dart_tool`, `node_modules`, test evidence.

- Explicit Allow paths from the user, if any:
  - None.

- Escalation triggers:
  - Need to execute `DRY_RUN=0`.
  - Need to inspect product/PDD alignment.
  - Need to modify runner, prompts, or artifacts.
  - Need to read more than representative workflow files.

## Review Scope

- Files expected to change:
  - None.

- Files Claude may need to read for review:
  - `tools/agent-review/runner/run_plan_review.sh`
  - `tools/agent-review/profiles/runiac/prompts/03_codex_final_review_decision.md`
  - `.claude/settings.json`

- Files explicitly out of scope:
  - Product requirements, PDD content, implementation source, Firebase files, tests, submitted assessment docs, PDFs, images, generated assets, secrets.

- Risk tags:
  - touches XP/streak/level/rank/leaderboard: no
  - touches Firebase ownership or Cloud Functions ownership: no
  - touches roles/entitlements/premium fairness: no
  - touches security rules: no
  - touches PRD/PDD consistency: no
  - touches production source code: no

- Recommended review mode: lite

## Proposed Plan

1. Confirm runner validation path:
   - `REVIEW_ENABLED` accepts only `1|0`.
   - `REVIEW_ENABLED=0` requires `SKIP_REASON`.

2. Confirm skipped-review artifact behavior:
   - `cmd_review` and `cmd_pipeline` use `new_skipped_review_output_file`.
   - Actual skipped artifact includes `Status: SKIPPED`, reason, context class, risk tags, and warning that skipped review is not approval.

3. Confirm Codex decision behavior:
   - Pipeline sets skipped artifact as `REVIEW_FILE`.
   - `run_decision_step` passes both `PLAN_FILE` and `REVIEW_FILE` to Codex.
   - Decision prompt explicitly handles `Status: SKIPPED` with elevated self-critique.

4. Smoke-test command, if later approved:
   - Use `DRY_RUN=1 REVIEW_ENABLED=0 SKIP_REASON="smoke test skipped Claude review"` with `pipeline`.
   - This should print the intended plan, skipped-review, and decision flow without writing artifacts or invoking Claude/Codex.

## Files Affected If Executed

None for this inspect-only plan.

If a later approved actual smoke test uses `DRY_RUN=0`, expected artifacts would be written under:

- `implementation/traceability/plans/`
- `implementation/traceability/reviews/`
- `implementation/traceability/decisions/`

## Files That Must Remain Untouched

- Flutter/Firebase/backend source
- Tests
- PDD deliverables
- Submitted docs/assets
- Secrets and `.env*`
- Existing traceability artifacts unless explicitly approved

## Risks

Low. This is workflow-only and inspect-only.

Main residual risk: the actual CLI smoke test was not executed, so this confirms behavior from script and prompt inspection rather than runtime output.

## Approval Gate

Ready for user approval to run a smoke test later, preferably dry-run only:

```sh
DRY_RUN=1 REVIEW_ENABLED=0 SKIP_REASON="smoke test skipped Claude review" TASK_PROMPT="REVIEW_ENABLED=0 smoke test only. Do not modify files." tools/agent-review/runner/run_plan_review.sh pipeline
```
