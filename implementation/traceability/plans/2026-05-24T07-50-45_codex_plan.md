## Goal

Use A0_ORCH to create a concise inspect-only plan for a workflow-scoped high-risk guard smoke test involving standalone context packets from `build_context_packet.sh` and optional use by `run_plan_review.sh` when `CONTEXT_PACKET_ENABLED=1`.

No files will be modified, created, deleted, staged, committed, tested, built, deployed, or production-affecting commands run.

## Repo Context Checked

- Current repo: `/Users/leejinseo/Desktop/FYP_Runiac`
- `git status --short` showed one pre-existing untracked file: `implementation/traceability/plans/2026-05-24T07-50-45_codex_plan.md`
- User-provided context packet says clean, but live repo status is not clean. Treat the untracked file as unrelated and leave it untouched.
- Checked only representative workflow/instruction files.

## Planning Evidence Read

- Files actually read
  - `AGENTS.md`
  - `CLAUDE.md`
  - `tools/agent-review/runner/classify_high_risk_task.sh`

- Files intentionally skipped
  - `tools/agent-review/runner/build_context_packet.sh`
  - `tools/agent-review/runner/run_plan_review.sh`
  - `tools/agent-review/runner/lib/common.sh`
  - Workflow prompts/configs beyond the high-risk classifier
  - Product docs, PDD docs, PRD, diagrams, images, PDFs, Flutter/Firebase source, tests, generated assets, evidence folders, secrets/env files

- Reason for skipping large/reference/irrelevant files
  - This is a workflow smoke-test plan with a user-declared `workflow` context.
  - The supplied context packet already provides inventory, scope, and policy context.
  - Reading more workflow files would exceed the compact smoke-test intent unless a standard review is approved.

## Context Class Decision

- Selected context class
  - `workflow`

- Reason
  - The task explicitly declares workflow context and targets runner/context-packet/high-risk guard behavior.

- Excluded classes and why
  - `docs`: no documentation deliverable is being edited.
  - `implementation_prep`: no implementation planning or scaffolding decision is requested.
  - `feature`: no product feature planning or implementation is requested.
  - `security`: security-sensitive invariants are checked as always-on constraints, but the task is workflow tooling only.
  - `architecture`: no system architecture or ownership model change is requested.
  - `unknown`: not applicable because the user declared the class.

- Whether user-declared or Codex-inferred
  - User-declared.

## Plan Scope

- Allowed planning files/paths
  - `AGENTS.md`
  - `CLAUDE.md`
  - `.claude/settings.json`
  - `tools/agent-review/**`

- Excluded planning files/paths
  - Product docs and submitted assessment docs
  - `docs/pdd/**`, unless explicitly allowed for PRD/PDD alignment
  - Flutter, Firebase, backend, tests, generated files, evidence outputs
  - `.env`, `.env.*`, `secrets/**`

- Large or expensive assets to avoid
  - `docs/submissions/**`
  - `**/*.pdf`
  - `**/*.png`
  - `**/*.jpg`
  - `**/*.jpeg`
  - `**/*.svg`
  - `**/node_modules/**`
  - `**/build/**`
  - `**/.dart_tool/**`
  - `test-evidence/**`

- Explicit Allow paths from the user, if any
  - `tools/agent-review/**`
  - `.claude/settings.json`
  - `AGENTS.md`
  - `CLAUDE.md`

- Escalation triggers
  - Need to inspect more than representative workflow files.
  - Need to run the smoke test rather than plan it.
  - Need to write traceability/evidence output.
  - Any proposed scope expansion into PRD/PDD, production source, Firebase, tests, or sensitive files.
  - Any review scope file outside allowed workflow paths.

## Review Scope

- Files expected to change
  - None.

- Files Claude may need to read for review
  - `CLAUDE.md`
  - `tools/agent-review/runner/classify_high_risk_task.sh`
  - `tools/agent-review/runner/run_plan_review.sh`

- Files explicitly out of scope
  - Product requirements, PDD deliverables, submitted assessment docs, diagrams/images/PDFs, Flutter/Firebase source, tests, generated assets, secrets/env files, and the unrelated untracked traceability plan.

- Risk tags:
  - touches XP/streak/level/rank/leaderboard: no
  - touches Firebase ownership or Cloud Functions ownership: no
  - touches roles/entitlements/premium fairness: no
  - touches security rules: no
  - touches PRD/PDD consistency: no
  - touches production source code: no

- Recommended review mode: lite

Review Scope consistency check: pass. All review files are within allowed workflow paths or always-read instruction files.

## Proposed Plan

1. Confirm the smoke-test remains inspect-only and workflow-scoped.
2. Verify the supplied standalone context packet declares `workflow`, constrained allowed paths, high-risk guard settings, and broad exclusions.
3. Inspect representative high-risk guard behavior in `classify_high_risk_task.sh`.
4. Check that inspect-only or negated risky wording is downgraded to warning only when no positive risky action is present.
5. Check that positive risky actions still block, especially Firebase init/deploy, build/test commands, secrets, PRD/PDD edits, XP/rank/leaderboard ownership violations, and approval bypass language.
6. Ask Claude for lite review using only the minimal review files above, if review execution is explicitly allowed later.
7. Codex final decision should be one of:
   - `PASS`: smoke-test plan is safe and scoped.
   - `DEFER`: more workflow files are needed, requiring standard review.
   - `BLOCK`: plan would exceed inspect-only or workflow scope.

## Files Affected If Executed

None. This plan is inspect-only.

## Files That Must Remain Untouched

- `implementation/traceability/plans/2026-05-24T07-50-45_codex_plan.md`
- `docs/pdd/**`
- `docs/submissions/**`
- `implementation/**`
- `firebase/**`
- `tests/**`
- `test-evidence/**`
- `.env`, `.env.*`, `secrets/**`
- Images, PDFs, generated build outputs, and dependency folders

## Risks

- Live git status differs from the supplied packet because an untracked traceability plan exists.
- Inspecting only the high-risk classifier may miss integration behavior in `run_plan_review.sh`; keep lite review representative or escalate to standard.
- Running the actual smoke test could create evidence or traceability output, so execution requires separate approval.

## Approval Gate

Ready for review planning only.

Do not execute the smoke test, modify workflow files, create evidence, stage, commit, run tests/builds, or expand review scope without explicit user approval.
