## Final Recommendation

Accept most of Claude’s feedback. The original plan is safe, but it overstates how generic the runner becomes in Batch 1 and misses concrete migration details.

Revised recommendation: proceed only if the user still wants profile migration now. If approved, Batch 1 should be tightened to update profile paths, define precedence, make fallback explicit and logged, preserve safety flags, and avoid writing smoke-test output into `implementation/traceability/`.

## Claude Feedback Decision Table

| Claude Feedback | Decision | Reason | Final Action |
| --- | --- | --- | --- |
| Keep in-repo, copy-first, gated deletions are sound. | Accept | Matches AGENTS commit/path protection and avoids risky deletes. | Keep copy-first migration and defer legacy removal. |
| Runner is not fully generic because it still has Runiac output dirs and root `CLAUDE.md`. | Accept | Current `run_plan_review.sh` confirms Runiac traceability defaults and `--append-system-prompt "$(cat CLAUDE.md)"`. | Reword acceptance check or make these profile/config-driven. Do not claim full genericity in Batch 1. |
| Profile env example must rewrite prompt paths, not verbatim copy legacy paths. | Accept | Current env example points at `tools/agent-review/prompts/runiac/...`; copied profile env would be wrong. | New profile env example must point at `tools/agent-review/profiles/runiac/prompts/...`. |
| Legacy fallback can silently mask bad profile configuration. | Accept | A permissive fallback could run stale prompts while appearing migrated. | Prefer fail-loud when a selected profile exists but files are missing; fallback only if explicit and logged. |
| Run dry-run smoke checks before commit. | Accept | Runner path changes should be verified before commit. | After approval, smoke-check subcommands with safe disposable output paths, not existing `implementation/traceability/`. |
| Define precedence between `AGENT_REVIEW_CONFIG` and profile variables. | Accept | Current runner only supports `AGENT_REVIEW_CONFIG`; adding profile variables needs deterministic behavior. | Document and implement precedence: explicit env/config overrides profile defaults. |
| Add `.gitignore` guidance for local env files. | Accept | Env examples are committed; real local env files should not be. | Check existing ignore rules first; add only a narrow ignore pattern if missing. |
| `profiles/` may be premature for one consumer. | Defer | Valid concern, but the requested plan is specifically about generic runner plus profile structure; in-repo profile migration is still less risky than externalizing. | Keep as an approval decision: user must confirm they want profile migration now. |
| Preserve Runiac AGENTS/PDD guardrails in prompts and README. | Accept | Required by root AGENTS.md and submitted-PDD constraints. | Copy/verify guardrails during migration. |
| Manual scoped commit only; do not stage unrelated traceability files. | Accept | AGENTS limits auto-commit and current git state has unrelated untracked files. | Provide explicit `git add` only after approved implementation. |

## Revised Final Plan

1. Keep the workflow inside `tools/agent-review/`; do not extract to another repo.

2. If the user approves profile migration, create `tools/agent-review/profiles/runiac/` with copied prompts, a profile README, and a rewritten profile env example.

3. Update the runner to support:
   - `AGENT_REVIEW_PROFILE`, default `runiac`
   - `AGENT_REVIEW_PROFILE_DIR`, default `tools/agent-review/profiles/$AGENT_REVIEW_PROFILE`
   - `AGENT_REVIEW_CONFIG` as an explicit override
   - profile prompt defaults
   - documented precedence: shell/env config overrides profile defaults

4. Keep safety flags unchanged:
   - plan/decision: `--sandbox read-only --ask-for-approval never`
   - Claude review: `--permission-mode plan --tools "Read,Grep,Glob"`
   - implementation: `--sandbox workspace-write --ask-for-approval on-request`

5. Treat root `CLAUDE.md` and traceability output dirs as transitional Runiac couplings unless moved into profile config during the same approved batch.

6. Avoid silent fallback. Missing selected profile files should fail clearly unless an explicit legacy fallback mode is documented and logged.

7. Before commit, run only approved smoke checks with disposable output paths. Do not create new traceability files in `implementation/traceability/` as part of verification unless explicitly approved.

## Files To Modify If Approved

```text
tools/agent-review/README.md
tools/agent-review/runner/run_plan_review.sh
tools/agent-review/profiles/runiac/README.md
tools/agent-review/profiles/runiac/agent-review.env.example
tools/agent-review/profiles/runiac/prompts/01_codex_create_plan.md
tools/agent-review/profiles/runiac/prompts/02_claude_review_plan.md
tools/agent-review/profiles/runiac/prompts/03_codex_final_review_decision.md
tools/agent-review/profiles/runiac/prompts/04_codex_implement_approved_plan.md
```

Optional only if needed and explicitly approved:

```text
.gitignore
tools/agent-review/config/runiac.agent-review.env.example
tools/agent-review/prompts/runiac/
CLAUDE.md
```

## Files To Keep Untouched

```text
PRD.md
docs/pdd/
docs/submissions/pdd/
docs/submissions/pdd/FYP-26-S2-38_Project_Design_Document_submitted_2026-05-23.pdf
implementation/mobile/
firebase/
tests/
test-evidence/
implementation/traceability/
```

Do not stage current unrelated untracked files:

```text
implementation/traceability/plans/2026-05-23T13-50-40_codex_plan.md
implementation/traceability/plans/2026-05-23T13-50-49_codex_plan.md
implementation/traceability/reviews/2026-05-23T13-52-48_claude_review.md
implementation/traceability/reviews/2026-05-23T13-52-52_claude_review.md
implementation/traceability/decisions/2026-05-23T13-56-47_codex_decision.md
implementation/traceability/decisions/2026-05-23T13-56-50_codex_decision.md
```

## Implementation Prompt For Later

Implement the approved `tools/agent-review` profile migration only. Create the `profiles/runiac` structure, rewrite profile prompt paths, add profile README guidance, update runner profile loading and precedence, keep safety flags unchanged, and make missing selected profile files fail clearly or log any explicit fallback. Do not modify PRD/PDD/submitted PDF/Flutter/Firebase/tests/traceability content. Do not stage or commit.

## User Approval Required

Approval is required before any implementation. No files were modified, staged, committed, tested, built, or deployed in this review.
