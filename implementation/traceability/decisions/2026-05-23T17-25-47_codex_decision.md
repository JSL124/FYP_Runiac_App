## Final Recommendation

Accept Claude’s review in substance and revise the plan to ship `REVIEW_MODE=standard|lite` only for the next implementation step. Defer `REVIEW_PROVIDER=none`.

Repo evidence supports Claude’s main concern: the current runner hardcodes Claude review filenames and wording in `run_plan_review.sh`, the decision prompt, and pipeline output. A no-review provider would require more governance design than the original plan allowed.

I performed inspect-only checks only. No files were modified, created, deleted, staged, committed, tested, built, or deployed.

## Claude Feedback Decision Table

| Claude Feedback | Decision | Reason | Final Action |
| --- | --- | --- | --- |
| Ship `lite` only; defer `REVIEW_PROVIDER=none` | Accept | The tool currently enforces an independent Claude review gate. `none` weakens that gate and needs more design. | Remove `REVIEW_PROVIDER=none` from the next implementation batch. |
| Manual/no-provider mode could satisfy review with an empty checklist | Accept | There is no human-completion gate in the current runner or original plan. | Defer manual checklist mode until a completion/approval gate is specified. |
| `_claude_review.md` artifact name is hardcoded | Accept | Current `new_review_output_file()` writes `*_claude_review.md`; this would mislabel manual output. | No immediate change because `none` is deferred; make provider-aware naming mandatory before any future `none` mode. |
| Decision prompt and pipeline text hardcode “Claude review” | Accept | Current prompt `03` and runner injection text explicitly say “Claude review.” | Keep as-is for lite-only because Claude still runs; require provider-aware wording before future `none`. |
| `REVIEW_MODE=lite` needs guardrails for high-risk Runiac rules | Accept | Lite review could miss XP, roles, entitlement, leaderboard, or safety-rule violations. | Add escalation wording to the lite prompt and docs: use standard mode for high-risk/non-negotiable Runiac changes. |
| `require_common_paths` branching could break standard mode | Accept | The original 2x2 plan required branching; lite-only still needs careful prompt validation. | Keep standard default intact; validate standard prompt remains required by default and lite prompt only when selected. |
| Verify Claude CLI `--tools` flag | Defer | This is pre-existing and cannot be verified without executing Claude/help checks, outside this review scope. | Keep the existing command unchanged; note verification for a later real-run environment. |
| Define `REVIEW_PROMPT` vs `REVIEW_PROMPT_LITE` precedence | Accept | Env override behavior affects predictable runner usage. | Document precedence: explicit `REVIEW_PROMPT` wins unless unset; `REVIEW_MODE=lite` selects the lite default only when no explicit review prompt is provided. |
| Prompt numbering jumps from `04` to `06`/`07` | Accept | With `none` deferred, `06_manual_review_checklist.md` should not be added now. | Add only `05_claude_review_plan_lite.md` or another consecutive lite filename. |
| Re-run git status and exclude all untracked traceability files | Accept | Current git state shows three untracked traceability artifacts, not one. | Future implementation must exclude all current untracked traceability files and avoid `git add .`. |
| `none + lite` / `none + standard` no-op matrix is redundant | Accept | The no-provider matrix is unnecessary once `none` is deferred. | Remove provider matrix from the next plan. |
| `chore(repo)` vs `docs(agents)` commit type | Accept | This is workflow tooling plus prompts/docs, not app implementation. | Prefer `chore(repo): add lite agent review mode`, with manual commit only. |

## Revised Final Plan

1. Implement only `REVIEW_MODE=standard|lite`.

2. Preserve current default behavior:
   - default `REVIEW_MODE=standard`
   - standard mode uses existing `02_claude_review_plan.md`
   - Claude still runs with the existing read-only restrictions
   - no manual/no-review provider is added

3. Add one lite Claude review prompt:
   - concise blockers/high-risk omissions/unsafe operations/final recommendation
   - explicit escalation instruction: use standard mode for changes touching XP, level, rank, streaks, leaderboard, roles, entitlements, premium fairness, Firebase ownership, Cloud Functions ownership, security rules, or submitted-PDD consistency

4. Update runner config:
   - add `REVIEW_MODE="${REVIEW_MODE:-standard}"`
   - validate only `standard` or `lite`
   - select the lite prompt only when `REVIEW_MODE=lite` and no explicit `REVIEW_PROMPT` override is set
   - preserve existing `REVIEW_PROMPT` override compatibility

5. Update docs and env example for lite mode only.

6. Acceptance checks for later approved implementation:
   - `bash -n tools/agent-review/runner/run_plan_review.sh`
   - dry-run pipeline default standard mode
   - dry-run pipeline with `REVIEW_MODE=lite`
   - inspect dry-run output to confirm Claude is still used in both modes

## Files To Modify If Approved

```text
tools/agent-review/runner/run_plan_review.sh
tools/agent-review/README.md
tools/agent-review/profiles/runiac/README.md
tools/agent-review/profiles/runiac/agent-review.env.example
tools/agent-review/profiles/runiac/prompts/05_claude_review_plan_lite.md
```

## Files To Keep Untouched

```text
PRD.md
docs/pdd/
docs/submissions/pdd/
implementation/traceability/
implementation/mobile/
firebase/
tests/
test-evidence/
```

Also keep untouched:

```text
implementation/traceability/decisions/2026-05-23T17-25-47_codex_decision.md
implementation/traceability/plans/2026-05-23T17-25-47_codex_plan.md
implementation/traceability/reviews/2026-05-23T17-25-47_claude_review.md
```

## Implementation Prompt For Later

Implement Agent Review Efficiency Batch B as lite-mode only.

Use A0_ORCH as workflow owner. Do not implement `REVIEW_PROVIDER=none` or manual checklist mode. Add `REVIEW_MODE=standard|lite`, default `standard`, preserving current Claude standard review behavior. Add a Runiac lite Claude review prompt and route to it only when `REVIEW_MODE=lite` and no explicit `REVIEW_PROMPT` override is set. Keep Claude permissions/tools unchanged. Update runner usage, profile README, root agent-review README, and env example. Do not touch PDD, PRD, Flutter, Firebase, tests, or existing traceability artifacts. Stop at Ready for commit.

## User Approval Required

Implementation is not approved yet. The next step requires explicit user approval before modifying files.
